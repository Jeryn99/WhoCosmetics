package mc.craig.software.cosmetics.compat;

/*
import net.minecraft.server.level.ServerLevel;
import net.minecraft.server.level.ServerPlayer;
import whocraft.tardis_refined.common.capability.TardisLevelOperator;
import whocraft.tardis_refined.common.network.messages.OpenMonitorMessage;
*/

public class TardisRefinedCompat {

/*    public static void openMonitor(ServerPlayer serverPlayer, ServerLevel serverLevel) {
        TardisLevelOperator.get(serverLevel).ifPresent(operator -> {
            (new OpenMonitorMessage(operator.getInteriorManager().isWaitingToGenerate(), operator.getExteriorManager().getLastKnownLocation(), operator.getControlManager().getTargetLocation())).send(serverPlayer);
        });
    }*/

}
